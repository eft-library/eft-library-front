import BoardMain from "./contents/boardMain";

export default function Board() {
  return <BoardMain />;
}

// 하나의 폼으로 모든 게시판에서 쓸 수 있게 하기 - 형태가 똑같아서 조회하는 타입만 다름
// main 파일을 만들고 한 번 감싸서 사용하기 - metaTag 사용해야 함
